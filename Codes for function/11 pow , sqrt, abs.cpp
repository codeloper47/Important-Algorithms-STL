#include <iostream>
#include <cmath>
using namespace std;

// sqrt , pow ,abs

int main() {
    int n=16;

    // sqrt
    cout<<sqrt(n)<<endl;;

    // power
    int a=5,b=3;
    cout<<pow(a,b)<<endl;

    // absolute mod
    int p=3,q=10;
    cout<<abs(p-q);
    
}
